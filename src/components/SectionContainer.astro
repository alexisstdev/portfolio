---
interface Props {
  title: string;
  id: string;
}

const { title, id } = Astro.props;
---

<section id={id}>
  <h2>{title}</h2>
  <slot />
</section>

<style>
  h2 {
    margin-top: 5rem;
    margin-bottom: 1rem;
    font-size: var(--text-3xl);
  }
</style>
